node {
    stage ('clone') {
        git branch: 'main', url: 'https://github.com/ganesh2425/git-final.git'
    }
    stage ('linux commands') {
        sh 'touch t2'
        sh 'mkdir ram'
        sh 'touch b1'
    }
}
